* add SMTP support, as so it does not require sendmail or any external
  programs to submit records.
* connect->isConnected() and consequences
* fix html encoding
* make cddb timeout configurable
* allow to choose email submisson address from server list entries with protocol SMTP
* allow user to type in CDDB server name

Juraj:
* add code, to remove the trailing CR character from track list,
  artist/title input box and from inexact dialog. It looks ugly.
* Figure out, why KFM writes KFM NOT READY, ERROR: KFM crashed, when
  trying to click on something in the nice i box ;-)
